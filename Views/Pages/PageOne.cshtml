@model List<MVCpractice2.Models.VisitorViewModel>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<div class="text-center">
    <h1 class="display-4">This is Page One</h1>
    <p>Still just practicing some MVC basics over here.</p>
    @if (Model.Count > 0)
        {
            <p>I wonder how you can put your name in that list down there??</p>
        }

    @if (Model.Count > 0)
    {
        @foreach (VisitorViewModel visitor in Model)
        {
            <ul>
                <li class="list-unstyled">@visitor.Name (@visitor.Age) says - "@visitor.Comment"</li>
            </ul>
        }
        
    }
</div>